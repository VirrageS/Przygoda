language: python
python:
  - 3.3
  - 3.4
  - 3.5
install:
  - pip install -r requirements.txt
  - pip install -r requirements-test.txt
  - pip install coveralls
script:
  - python tests.py
after_success:
  - coveralls
