language: python
python:
  - 2.6
  - 2.7
  - 3.2
  - 3.3
  - 3.4
install:
  - pip install -r requirements.txt
  - pip install unittest
  - pip install coveralls
script:
  - python tests.py
after_success:
  - coveralls
